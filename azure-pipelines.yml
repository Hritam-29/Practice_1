trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
- stage: Deploy
  displayName: 'Deploy to hb_demo'
  jobs:
  - deployment: DeployJob
    displayName: 'Deploy job for hb_demo'
    environment: hb_demo  # This binds the job to the 'hb_demo' environment in Azure DevOps
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "Starting deployment to hb_demo environment..."
            displayName: 'Echo start message'
          - script: |
              echo "Running deployment tasks..."
              echo "This is where your actual deployment logic would go."
            displayName: 'Simulate deployment'
